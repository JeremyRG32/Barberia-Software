@model List<Barberia.Web.Models.Usuario>



@{
    ViewData["Title"] = "Usuarios";
}
<<<<<<< Updated upstream:src/BarberiaSoftware/Barberia.Web/Views/Clientes/Index.cshtml

=======
>>>>>>> Stashed changes:src/BarberiaSoftware/Barberia.Web/Views/Usuarios/Index.cshtml


<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Lista de Usuarios</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-person-plus-fill"></i> Nuevo Usuario
        </a>

    </div>



    <div class="table-responsive">

        <table class="table table-striped table-bordered shadow-sm">

            <thead class="table-dark">

                <tr>

                    <th>ID</th>

                    <th>Nombre</th>

                    <th>Email</th>

                    <th>Teléfono</th>

                    <th>Dirección</th>

                    <th>Estado</th>

                    <th>Locales Favoritos</th>

                    <th class="text-center">Acciones</th>

                </tr>

            </thead>

            <tbody>
                @foreach (var usuario in Model)
                {

                    <tr>
                        <td>@usuario.Id</td>
                        <td>@usuario.Nombre</td>
                        <td>@usuario.Email</td>
                        <td>@usuario.Telefono</td>
                        <td>@usuario.Direccion</td>
                        <td>@usuario.Estado.ToString()</td>
                        <td>
                            @if (usuario.LocalesFavoritos != null && usuario.LocalesFavoritos.Any())
                            {

                                <ul class="mb-0 ps-3">
                                    @foreach (var local in usuario.LocalesFavoritos)
                                    {

                                        <li>@local.Nombre</li>

                                    }

                                </ul>

                            }

                            else

                            {

                                <span class="text-muted">Ninguno</span>

                            }

                        </td>

                        <td class="text-center">
                            <a asp-action="Details" asp-route-id="@usuario.Id" class="btn btn-sm btn-info me-1">
                                <i class="bi bi-eye"></i>

                            </a>
                            <a asp-action="Edit" asp-route-id="@usuario.Id" class="btn btn-sm btn-warning me-1">
                                <i class="bi bi-pencil-square"></i>

                            </a>
                            <a asp-action="Delete" asp-route-id="@usuario.Id" class="btn btn-sm btn-danger">
                                <i class="bi bi-trash"></i>

                            </a>

                        </td>

                    </tr>

                }

            </tbody>

        </table>

    </div>

</div>